# Basic requirements for telegrip package
# For development with pinned versions, use: pip install -r requirements-dev.txt
# For installation as package, use: pip install -e .

numpy
# torch  # 注释掉：aider_terminal 不需要 PyTorch（仅在 open_origin/lerobot 示例中使用）
scipy
pybullet  # 必需：用于 IK（逆运动学）计算
websockets
pynput
aiortc
requests
opencv-python
# RobStride 灵足电机驱动（基于官方 el_a3_sdk，无需额外 pip 安装）
# 注意：需要配置 SocketCAN 接口和 Canable USB2CAN 适配器
# 系统依赖: sudo apt-get install can-utils
# CAN 配置: sudo ip link set can0 type can bitrate 1000000 && sudo ip link set can0 up
# lerobot[feetech]  # 注释掉，使用 drivers/so_follower.py 的空实现
# For development
pytest
black
flake8
pyyaml>=6.0
# ===================================================================
# pink IK solver dependencies (for test_openarm_pink_ik.py)
# ===================================================================
# 安装顺序(Windows, aider conda 环境):
#   1. conda install -c conda-forge pinocchio -y       # 必须先装
#   2. pip install meshcat_shapes qpsolvers loop_rate_limiters quadprog
#   3. pip install pin-pink --no-deps                   # 跳过 pin, 用 conda 的 pinocchio
meshcat_shapes         # MeshCat 可视化辅助(画坐标系、Frame)
qpsolvers              # 二次规划(QP)求解器封装
loop_rate_limiters     # 循环频率限制器
quadprog               # QP 求解器后端(脚本默认使用)
pin-pink               # pink IK 求解框架 (--no-deps 安装, pin 由 conda pinocchio 提供)
# pinocchio              # conda 安装: conda install -c conda-forge pinocchio -y
#                          pip 安装需要 CMake + C++ 编译器, Windows 上不可行