# Development environment with pinned versions
# Install with: pip install -r requirements-dev.txt

# Core dependencies
numpy==1.21.0
torch==2.0.1
scipy==1.9.0
pybullet==3.2.5
websockets==11.0.3
pynput==1.7.6
feetech-servo-sdk==1.0.0  # ST3215总线舵机驱动SDK
# el_a3_sdk  # Robstride灵足电机驱动SDK (Linux only, 需要手动安装: cd /path/to/EDULITE_A3/el_a3_sdk && pip install -e .)
# lerobot[feetech]==0.1.0  # 已替换为 feetech-servo-sdk

# Development tools
pytest==7.4.0
black==23.3.0
flake8==6.0.0
mypy==1.4.1 