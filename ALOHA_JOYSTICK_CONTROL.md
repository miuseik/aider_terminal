# VR 摇杆控制 Aloha 底盘说明

## 功能概述

通过 VR 控制器的摇杆来控制 Aloha 可升降底盘的运动。

## 摇杆映射

### 左手控制器摇杆
- **Y轴 (上下)**: 控制底盘前进/后退
  - 向上推: 前进
  - 向下拉: 后退
  - 速度: 0.02米/次更新

- **X轴 (左右)**: 预留用于转向(暂未实现)

### 右手控制器摇杆
- **X轴 (左右)**: 控制底盘左右平移
  - 向左推: 左移
  - 向右推: 右移
  - 速度: 0.02米/次更新

- **Y轴 (上下)**: 控制底盘升降
  - 向上推: 下降
  - 向下拉: 上升
  - 速度: 0.01米/次更新
  - 范围: 0.0 ~ 0.7854米 (由 URDF 限制)

## 技术实现

### 前端 (vr_app.js)
1. 读取 VR 控制器的 gamepad 数据
2. 获取摇杆的 X/Y 轴值 (范围 -1.0 到 1.0)
3. 将摇杆数据添加到 WebSocket 消息中发送给后端

### 后端 (vr_ws_server.py)
1. 接收摇杆数据
2. 应用死区过滤 (dead zone: 0.1) 避免漂移
3. 根据摇杆输入计算位置变化
4. 发送高度控制命令到控制循环

### 控制循环 (control_loop.py)
1. 接收高度控制命令
2. 调用 visualizer 的 `set_aloha_height()` 方法
3. 更新 PyBullet 仿真中的 Aloha 底盘高度

## 配置

在 `config.yaml` 中可以配置:

```yaml
aloha:
  enabled: true              # 是否启用 Aloha 底盘
  initial_height: 0.3        # 初始高度(米)
```

## 调试

查看日志输出:
```bash
python main.py --no-robot --log-level debug
```

相关日志关键词:
- `Aloha height`: 高度变化
- `Aloha X position`: X 轴位置
- `Aloha Y position`: Y 轴位置

## 注意事项

1. **死区设置**: 当前死区为 0.1,如果摇杆有漂移可以增大此值
2. **速度调整**: 可以通过修改 `move_speed`、`turn_speed`、`height_speed` 参数调整灵敏度
3. **高度限制**: 底盘高度被限制在 0.0 ~ 0.7854 米之间,超出范围会自动截断
4. **固定底座**: Aloha 底盘使用 `useFixedBase=1`,不会受重力影响下落

## 未来扩展

可以添加的功能:
- [ ] 左摇杆 X 轴控制底盘旋转
- [ ] 保存/加载底盘位置预设
- [ ] 可视化显示底盘当前位置
- [ ] 添加碰撞检测
- [ ] 支持真实硬件控制
